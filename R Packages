## Assigning a variable use <- It works only in R 

# version control
h <- --version

# Variable are assigned using
  ' <- ' this symbol

# You can print by just entering the name of variable.

# Many packages